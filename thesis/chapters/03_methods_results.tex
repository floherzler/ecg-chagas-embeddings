\chapter{Methods and Results}
\label{ch:methods-results}

\section{Configuration System}
Experiments are implemented in PyTorch \parencite{pytorch_2019} and organized using PyTorch Lightning \parencite{pytorch_lightning_2025}.
All runs are configured through composable YAML files using a base configuration plus a track-specific configuration:
\begin{itemize}
  \setlength{\itemsep}{0.1\baselineskip}
  \setlength{\topsep}{0.25\baselineskip}
  \setlength{\partopsep}{0pt}
  \setlength{\parskip}{0pt}
  \setlength{\parsep}{0pt}
  \item \texttt{configs/base.yaml}: shared defaults (data loading, model backbone, optimizer/scheduler, default augmentations, W\&B logger).
  \item \texttt{configs/track1.yaml}: Track~1 callbacks and model toggles for classification.
  \item \texttt{configs/track2\_sup\_standard.yaml}, \texttt{configs/track2\_sup\_min.yaml}, \texttt{configs/track2\_sup\_proto.yaml}: Track~2 variants for embedding learning.
  \item \texttt{configs/track3\_probe.yaml}: Track~3 configuration for probing a pretrained encoder.
\end{itemize}
An individual run typically combines \texttt{base.yaml} + one track config + an optional loss config (e.g., \texttt{configs/losses/*.yaml}) and then overrides a small number of parameters on the command line for the specific experiment.

\Needspace*{6\baselineskip}
\section{Model Architecture}

All tracks share the same backbone encoder for fair comparison.
The implementation is named \texttt{resnet18\_ecg\_flex} because it was developed during earlier PhysioNet Challenge experimentation and supports swapping blocks, normalization layers, and optional modules.
For this thesis, the architecture is intentionally \emph{fixed across all tracks} to keep comparisons focused on preprocessing, augmentation, and objective choices rather than model capacity.

\Needspace*{28\baselineskip}
\begin{wrapfigure}{r}{0.44\textwidth}
\centering
\begin{minipage}{0.44\textwidth}
\resizebox{\linewidth}{!}{%
\begin{tikzpicture}[node distance=4mm,>=latex]
\tikzset{
  arrow/.style={-Latex, thick},
  annot/.style={font=\footnotesize},
  op/.style={draw, rounded corners=2pt, thick, align=center, minimum height=5mm, minimum width=18mm, fill=gray!10},
  stem/.style={draw, rounded corners=2pt, thick, align=center, minimum height=5mm, minimum width=22mm, fill=gray!10},
  blk/.style={draw, rounded corners=2pt, thick, align=center, minimum height=5mm, minimum width=18mm},
  head/.style={draw, rounded corners=2pt, thick, align=center, minimum height=8mm, minimum width=18mm},
  tiny/.style={draw, rounded corners=2pt, thick, align=center, minimum height=4.5mm, minimum width=16mm, font=\footnotesize},
}
% =========================================================
% Overview
% =========================================================
\node[op] (x) {12-lead ECG};
\node[stem,below=3mm of x] (conv) {Conv-BN-ReLU\\MaxPool};
\node[blk,below=3mm of conv,fill=orange!20] (resblk) {ResBlk $\times 4$};
\node[op,below=3mm of resblk] (drop) {GAP-Flatten\\Dropout};

    \draw[arrow] (x) -- (conv);
    \draw[arrow] (conv) -- (resblk);
    \draw[arrow] (resblk) -- (drop);

    % --- split to heads without overlap ---
\node[head,below=7mm of drop, xshift=-10mm, anchor=north,fill=blue!30] (proj) {Projection\\Head};
\node[head,below=7mm of drop, xshift= 10mm, anchor=north,fill=blue!30] (clf)  {Classification\\Head};
    \draw[arrow] (drop) -- (proj);
    \draw[arrow] (drop) -- (clf);

    % =========================================================
    % Residual block (zoom)
    % =========================================================
\node[draw, rounded corners=3pt, thick, inner sep=5pt, fill=orange!20,
    right=10mm of resblk, yshift=-6mm,
    minimum width=23mm, minimum height=48mm] (inset) {};
\node[annot,font=\bfseries,anchor=north] (t_res)
  at ($(inset.north)+(0,-0.5mm)$) {ResBlk};

    \node[tiny,fill=blue!10,below=3mm of inset.north] (c1) {Conv $3{\times}1$};
    \node[tiny,fill=blue!10,below=3mm of c1] (bn1) {BN};
    \node[tiny,fill=red!20,below=3mm of bn1] (r1) {ReLU};
    \node[tiny,fill=blue!10,below=3mm of r1] (c2) {Conv $3{\times}1$};
    \node[tiny,fill=blue!10,below=3mm of c2] (bn2) {BN};
    \node[tiny,fill=red!20,below=3mm of bn2] (rout) {ReLU};

    \coordinate (inflow) at ($(c1.north)+(0,3mm)$);
    \draw[arrow] (inflow) -- (c1.north);
    \draw[arrow] (c1) -- (bn1);
    \draw[arrow] (bn1) -- (r1);
    \draw[arrow] (r1) -- (c2);
    \draw[arrow] (c2) -- (bn2);
    \draw[arrow] (bn2) -- (rout);

    % skip connection (identity / optional downsample), merges before final ReLU
\draw[very thick,-Latex]
  ($(inflow)+(0,-1.5mm)$) |- ($(inflow)+(-8mm,-1.5mm)$) |- ($(rout.west)+(-3.5mm,0)$) -- (rout.west);
\end{tikzpicture}
}%
\end{minipage}
\caption[Model architecture]{\raggedright Model architecture with classification and projection heads (left) and the residual block definition (right). Batch normalization is used and squeeze--excitation blocks are disabled in all thesis runs.}
\label{fig:model_arch}
\end{wrapfigure}

We use a 1D adaptation of the ResNet family \parencite{resnet_2015} that operates directly on multi-lead ECG waveforms (Figure~\ref{fig:model_arch}).
The encoder follows the standard ResNet-18 stage pattern, but with 1D convolutions and temporal downsampling, and aggregates the final feature map using global average pooling.
Track~1 and Track~3 attach a classification head to these pooled features, while Track~2 adds a projection head and optimizes a contrastive/prototype objective on augmented views.

In the thesis experiments, we keep the backbone configuration fixed:
\begin{itemize}
  \item \textbf{Input}: 12 channels (standard 12-lead ECG), cropped to a fixed-length segment.
  \item \textbf{Backbone}: ResNet-18 style configuration with four residual stages (basic blocks, two blocks per stage), implemented with 1D convolutions, downsampling via strided convolutions and max-pooling, and global average pooling.
  \item \textbf{Normalization}: batch normalization (fixed for all thesis runs; group normalization is not used).
  \item \textbf{Squeeze--Excitation}: disabled in all thesis runs.
  \item \textbf{Heads}: a classification head (Track~1/3) and a projection head for supervised contrastive objectives (Track~2).
\end{itemize}
This architecture provides a strong and stable baseline while keeping the experimental focus on preprocessing, augmentations, and loss/objective choices rather than model capacity.

\section{Data Regimes}
We run the same training recipe on different preprocessed data folders to isolate the effect of harmonization:
\begin{itemize}
  \item \textbf{bp}: band-pass filtered signals.
  \item \textbf{bp\_sc}: band-pass + soft clipping.
  \item \textbf{bp\_sc\_norm}: band-pass + soft clipping + robust per-lead normalization.
\end{itemize}
For computational efficiency, experiments are executed from disk using the preprocessed folders.

\section{Augmentations}
\subsection{Default ``mild'' augmentations}
Unless a track-specific config overrides them, both Track~1 and Track~2 use the same mild augmentations defined in \texttt{configs/base.yaml}. These include small amplitude scaling and low-variance additive noise, while masking and strong temporal warps are disabled by default.

\subsection{Physiological augmentation: frontal axis rotation}
We optionally apply a physiologically motivated augmentation that rotates the ECG's frontal electrical axis by a small random angle. This augmentation is only enabled for \textbf{bp} signals because later preprocessing steps (soft clipping and normalization) are nonlinear and can break the assumptions required for a physically meaningful rotation.
Conceptually, this corresponds to changing the projection of the cardiac dipole onto the limb-lead axes in the frontal plane (often visualized via the Cabrera circle / hexaxial reference system), which induces coordinated amplitude and polarity changes across the limb leads.
The augmentation is controlled by:
\begin{itemize}
  \item \texttt{data.axis\_rotation\_max\_deg}: maximum rotation magnitude (degrees).
  \item \texttt{data.axis\_rotation\_prob}: probability of applying the rotation per sample.
  \item \texttt{data.per\_view\_axis\_rotation}: whether two contrastive views receive independent rotations.
\end{itemize}

\section{Track Definitions and Experiment Variables}
\label{sec:track-definitions}
\subsection{Track 1: supervised classification}
Track~1 trains a binary classifier on top of the backbone encoder. The main experimental variables are:
\begin{itemize}
  \item \textbf{Loss function}: weighted BCE, focal loss, and RAT.
  \item \textbf{Preprocessing regime}: \texttt{bp}, \texttt{bp\_sc}, \texttt{bp\_sc\_norm}.
  \item \textbf{Augmentation scheme}: baseline mild augmentations, optionally with axis rotation for \texttt{bp}.
\end{itemize}
We log the challenge-aligned score (TPR@5\%) and additional threshold-independent metrics (AUROC, AP, and pAUC@5\% FPR) for interpretable reporting.
Early stopping uses a 3-epoch rolling mean of AP (\texttt{val/ap\_mean3}) to reduce noise on imbalanced validation splits.

\subsection{Track 2: projection / embedding learning}
Track~2 optimizes representation geometry. We run three variants:
\begin{itemize}
  \item \textbf{SupCon (standard)}: baseline supervised contrastive loss.
  \item \textbf{SupCon (imbalance-aware)}: a variant that applies imbalance-handling logic in the supervised contrastive objective.
  \item \textbf{Prototype-based}: class prototypes provide additional structure in the embedding space.
\end{itemize}
Across all tracks, we log TTC embedding diagnostics (SAD, SAA, CAD, CAC, and GPU; Section~\ref{sec:ttc-metrics}) to quantify how different objectives, loss functions, and preprocessing regimes shape representation geometry.
In Track~2, these diagnostics are used to compare embedding structure across objectives and to justify when representation learning translates into improved downstream classification in Track~3.
Early stopping for Track~2 monitors a smoothed embedding-space metric (\texttt{emb\_CAC\_mean3}), and UMAP plots are used for qualitative inspection.

\subsection{Track 3: classification with pretrained encoder}
Track~3 reuses the encoder from Track~2 and trains a lightweight classifier (linear probe by default). This isolates representation quality from end-to-end supervised optimization and connects embedding-space improvements to downstream performance.

\section{Dataset Usage and Data Splits}
Ideally, model selection would be guided by an \emph{external} Chagas-labeled dataset for evaluation.
Since such a dataset was not available within the scope of this thesis, we instead created a single unified dataset and split it into five folds (0--4).
Folds 0--3 are used for training and validation (including cross-validation), while fold 4 is reserved as a held-out test set.

\subsection{Fold construction (patient-wise, label--source stratified)}
Fold assignment is performed during dataset preparation (see \texttt{prepare\_dataset.py}) using a stratified splitting scheme.
To avoid leakage across train/validation/test, folds are grouped by patient identifier when available (patient-wise splitting).
To keep both \emph{label} distribution and \emph{dataset/source} composition stable across folds, the stratification target is constructed from the combination of the Chagas label and the source indicator:
\begin{itemize}
  \item \textbf{Group}: \texttt{patient\_id} (all records of a patient are assigned to the same fold).
  \item \textbf{Strata}: \texttt{(chagas, source)} (implemented by concatenating label and a categorical source code).
  \item \textbf{Randomization}: shuffling with a fixed random seed for reproducibility.
\end{itemize}
This design yields folds that are comparable with respect to overall source distribution and (within sources that contain both labels) class imbalance.

\subsection{Screening split and cross-validation}
Due to computational constraints, the main ablation study is run on a single fixed split for efficient screening (train folds 0--2, validation fold 3), while keeping the held-out test fold 4 untouched.
Full 4-fold cross-validation over folds 0--3 is planned for selected configurations, and final reporting should include a single evaluation on fold 4 as an unbiased test.

\section{Experiment Execution}
Experiments are launched via SLURM scripts in \texttt{scripts/experiments/}. Each script composes the relevant configs and sets consistent run naming for W\&B.
Most hyperparameters are kept constant across runs (e.g., batch size), while the script selects the loss config and data regime and optionally enables axis rotation.
